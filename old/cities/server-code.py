#!/usr/bin/python
import random
def generateBackboneLinks():
	pairs = [[node['name']] for node in ixps]
	links = []
	while len(pairs) > 1:
		for i in range()
		if len(pairs) % 2 == 1:
			
			nextPairs = [pairs[i] + pairs[i + 1] for i in range(0, len(pairs) - 1, 2)] + pairs[len(pairs - 1)]
		else:
			nextPairs = [pairs[i] + pairs[i + 1] for i in range(0, len(pairs), 2)]
